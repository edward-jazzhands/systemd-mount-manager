[project]
name = "systemd-mount-manager"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "~=3.12.0"
dependencies = [
    "click==8.1.6",
    "textual>=6.11.0",
]

[dependency-groups]
dev = [
    "textual-dev>=1.8.0",
]
